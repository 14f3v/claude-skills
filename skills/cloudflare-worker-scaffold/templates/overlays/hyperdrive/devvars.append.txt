
# --- Hyperdrive (local Postgres for `wrangler dev`) ---
# By default `wrangler dev` uses the localConnectionString set in wrangler.jsonc.
# To override where it connects (takes precedence), export this in your shell
# before `npm run dev`:
#   export CLOUDFLARE_HYPERDRIVE_LOCAL_CONNECTION_STRING_HYPERDRIVE="postgres://postgres:postgres@localhost:5432/postgres"
